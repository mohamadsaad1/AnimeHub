
<% if (!user) { %>
    <link rel="stylesheet" href="/stylesheets/siwg.css">
  <% } %>
</head>
<body>
  <nav id="orange">
    <% if (user) { %>
      <a href="/auth/logout">
        <button type="submit">Logout</button>
      </a>
      <a href="/profiles"><button class="button1"> Profiles </button></a>
      <a href="/profiles/<%= user.profile._id%>/anime"><button class="buttons"> My Anime</button></a>
      
      <% } else { %>
        <a href="/auth/google" class="siwg-link">
          <button type="submit" class="siwg">
            <img id="siwg-image" alt="Sign in with Google" />
          </button>
        </a>
        <% } %>
        <div class="box">
          <form action="/animeSearch" method="POST">
            <input class="input" name= "query" type="text"  placeholder="Search..." onmouseout="document.search.txt.value = ''">
          </form>
        </div>
      </nav>
